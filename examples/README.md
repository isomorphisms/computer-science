# Examples

Small end-to-end problems used to test whether primitive descriptions, algorithms, calculations, cached observations, and the planner can actually derive useful implementation architectures.
