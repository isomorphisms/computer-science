# Measurements

Store empirical observations about candidate implementations and target machines: latency, throughput, memory, code size, energy, numerical error, startup cost, and other measured properties.

Measurements should be reusable evidence for later architecture searches rather than facts hidden inside one compiler run.
