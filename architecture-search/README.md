# Architecture search

A small executable home for treating implementation architecture as a search problem over machine primitives, known algorithms, measurements, and explicit requirements.

This starts with the SURFER question: given desired mathematical behavior and a concrete Android target, how much of the implementation architecture can the computer derive rather than requiring the programmer to choose it manually?
