# SURFER architecture-search example

Starting question:

Given a polynomial zero set, Android touch input, a concrete CPU/GPU target, and an interactive-frame requirement, construct and compare viable rendering architectures instead of requiring the programmer to choose the architecture manually.

This example should eventually connect the primitive catalog, algorithm catalog, measurements, and planner in one reproducible problem.
