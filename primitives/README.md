# Primitives

Describe what a target machine or platform can actually do: operations, inputs, outputs, contracts, restrictions, and quantities that can be calculated or measured.

A concrete primitive family should get its own directory with explicit files, rather than an `android-touch.*`-style wildcard name. Initial families include Android touch input, CPU instruction sets, and GPU / graphics interfaces.
